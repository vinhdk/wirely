import { ExistedReportNameDirective } from './existed-report-name.directive';

describe('ExistedReportNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistedReportNameDirective();
    expect(directive).toBeTruthy();
  });
});
